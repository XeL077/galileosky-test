/**
 * Parse the text area value to array
 * @param textAreaValue - string
 * @returns array
 */
export const toParseTextAreaValue = (textAreaValue: string) => {
    return textAreaValue.split(' ');
}